services:
  nvidia-smi-service:
    build: .
    container_name: nvidia-smi-service
    restart: unless-stopped
    ports:
      - "8765:8765"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
